import * as React from 'react';
import ReactDOM from 'react-dom/client';
import { Beef } from './components';

const root = ReactDOM.createRoot(
    document.getElementById('root') as HTMLElement,
);

root.render(<Beef />);